export class User {
  // email: string;
  // password: string;
  // name: string;

  constructor(public email: string,
              public password: string,
              public name?: string) { }
}
